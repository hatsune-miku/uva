# uva Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> **Git note:** The user forbids `git commit`. Where the template says "commit", instead `git add` the files and leave committing to the user.

**Goal:** Build `uva`, a thin Rust CLI that wraps `uv` to give Python a yarn-like, venv-free experience.

**Architecture:** A pure decision layer (`cli` + `detect`) turns args + filesystem state into a `Plan` value; a thin executor (`runner`) checks `uv` is present, echoes the command, spawns `uv` with inherited stdio, and propagates the exit code. Decisions are data, so the whole decision layer is unit-testable without subprocesses.

**Tech Stack:** Rust 2021 (cargo), no runtime dependencies; `tempfile` as the only dev-dependency.

---

### Task 1: Cargo scaffolding

**Files:**
- Create: `Cargo.toml`
- Create: `.gitignore`
- Create: `src/main.rs` (temporary stub)

- [ ] **Step 1: Create `Cargo.toml`**

```toml
[package]
name = "uva"
version = "0.1.0"
edition = "2021"
description = "uv automations — a yarn-like wrapper around uv"
license = "MIT"
repository = "https://github.com/hatsune-miku/uva"

[[bin]]
name = "uva"
path = "src/main.rs"

[dev-dependencies]
tempfile = "3"
```

- [ ] **Step 2: Create `.gitignore`**

```
/target
```

- [ ] **Step 3: Stub `src/main.rs`** so the project builds

```rust
fn main() {}
```

- [ ] **Step 4: Verify build**

Run: `cargo build`
Expected: compiles successfully.

---

### Task 2: `plan.rs` — the Plan contract

**Files:**
- Create: `src/plan.rs`

- [ ] **Step 1: Write `src/plan.rs`**

```rust
//! `Plan` is the contract between the pure decision layer (`cli`/`detect`)
//! and the side-effecting executor (`runner`). Decisions are data.

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum Plan {
    /// Run each `uv` command in order, stopping at the first failure.
    RunUv(Vec<UvCmd>),
    /// Print the uv install URL to stdout, exit 0.
    PrintUrl,
    /// Print usage to stdout, exit 0.
    Help,
    /// Print version to stdout, exit 0.
    Version,
    /// Print usage to stderr, exit 2.
    Usage,
    /// Print an error message to stderr, exit 1.
    Fail(String),
}

/// A single `uv` invocation.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct UvCmd {
    /// Arguments passed to `uv` (excluding the program name).
    pub args: Vec<String>,
    /// When true, the executor skips this command if `.venv` already exists.
    pub skip_if_venv_exists: bool,
}

impl UvCmd {
    pub fn new<I, S>(args: I) -> Self
    where
        I: IntoIterator<Item = S>,
        S: Into<String>,
    {
        UvCmd {
            args: args.into_iter().map(Into::into).collect(),
            skip_if_venv_exists: false,
        }
    }

    pub fn skip_if_venv_exists(mut self) -> Self {
        self.skip_if_venv_exists = true;
        self
    }
}
```

- [ ] **Step 2: Register the module** — add `mod plan;` to `src/main.rs` later (done in Task 6).

---

### Task 3: `detect.rs` — dependency detection + script resolution (TDD)

**Files:**
- Create: `src/detect.rs`

- [ ] **Step 1: Write `src/detect.rs` with the failing tests first**

```rust
//! Pure filesystem inspection: decide the install plan and resolve the
//! default `run` script. Every function takes the directory explicitly so it
//! can be unit-tested against a temp dir.

use crate::plan::{Plan, UvCmd};
use std::path::Path;

/// Build the install plan by inspecting `dir` for dependency files.
pub fn install_plan(dir: &Path) -> Plan {
    if dir.join("uv.lock").is_file() || dir.join("pyproject.toml").is_file() {
        Plan::RunUv(vec![UvCmd::new(["sync"])])
    } else if dir.join("requirements.txt").is_file() {
        Plan::RunUv(vec![
            UvCmd::new(["venv"]).skip_if_venv_exists(),
            UvCmd::new(["pip", "install", "-r", "requirements.txt"]),
        ])
    } else {
        Plan::Fail("当前目录不是一个 Python 项目".to_string())
    }
}

/// Build the `run` plan: `uv run <file> [extra...]`. When `filename` is None,
/// resolve the default script; on failure, return a Fail plan listing attempts.
pub fn run_plan(dir: &Path, filename: Option<&str>, extra: &[String]) -> Plan {
    let file = match filename {
        Some(f) => f.to_string(),
        None => match resolve_default_script(dir) {
            Ok(f) => f,
            Err(attempts) => {
                return Plan::Fail(format!(
                    "未指定源文件。已尝试过：{}",
                    attempts.join("、")
                ));
            }
        },
    };
    let mut args = vec!["run".to_string(), file];
    args.extend(extra.iter().cloned());
    Plan::RunUv(vec![UvCmd::new(args)])
}

/// Resolve the default script when no filename is given.
/// Ok(relative path string) or Err(list of attempted locations).
pub fn resolve_default_script(dir: &Path) -> Result<String, Vec<String>> {
    let mut attempts = Vec::new();

    attempts.push("当前目录下唯一的 .py 文件".to_string());
    if let Some(name) = single_py_file(dir) {
        return Ok(name);
    }

    attempts.push("src/ 下唯一的 .py 文件".to_string());
    if let Some(name) = single_py_file(&dir.join("src")) {
        return Ok(format!("src/{}", name));
    }

    attempts.push("main.py".to_string());
    if dir.join("main.py").is_file() {
        return Ok("main.py".to_string());
    }

    attempts.push("src/main.py".to_string());
    if dir.join("src").join("main.py").is_file() {
        return Ok("src/main.py".to_string());
    }

    Err(attempts)
}

/// Return the file name of the sole `*.py` file in `dir`, or None if there are
/// zero or more than one (or the dir can't be read).
fn single_py_file(dir: &Path) -> Option<String> {
    let mut found: Option<String> = None;
    for entry in std::fs::read_dir(dir).ok()?.flatten() {
        let path = entry.path();
        if path.is_file() && path.extension().and_then(|e| e.to_str()) == Some("py") {
            if found.is_some() {
                return None;
            }
            found = Some(entry.file_name().to_string_lossy().into_owned());
        }
    }
    found
}

#[cfg(test)]
mod tests {
    use super::*;
    use std::fs;
    use tempfile::tempdir;

    #[test]
    fn install_prefers_uv_lock() {
        let dir = tempdir().unwrap();
        fs::write(dir.path().join("uv.lock"), "").unwrap();
        assert_eq!(install_plan(dir.path()), Plan::RunUv(vec![UvCmd::new(["sync"])]));
    }

    #[test]
    fn install_uses_pyproject() {
        let dir = tempdir().unwrap();
        fs::write(dir.path().join("pyproject.toml"), "").unwrap();
        assert_eq!(install_plan(dir.path()), Plan::RunUv(vec![UvCmd::new(["sync"])]));
    }

    #[test]
    fn install_requirements_txt() {
        let dir = tempdir().unwrap();
        fs::write(dir.path().join("requirements.txt"), "").unwrap();
        assert_eq!(
            install_plan(dir.path()),
            Plan::RunUv(vec![
                UvCmd::new(["venv"]).skip_if_venv_exists(),
                UvCmd::new(["pip", "install", "-r", "requirements.txt"]),
            ])
        );
    }

    #[test]
    fn install_no_files_fails() {
        let dir = tempdir().unwrap();
        match install_plan(dir.path()) {
            Plan::Fail(msg) => assert!(msg.contains("Python")),
            other => panic!("expected Fail, got {:?}", other),
        }
    }

    #[test]
    fn resolve_single_py_in_cwd() {
        let dir = tempdir().unwrap();
        fs::write(dir.path().join("app.py"), "").unwrap();
        assert_eq!(resolve_default_script(dir.path()), Ok("app.py".to_string()));
    }

    #[test]
    fn resolve_multiple_py_falls_through_to_main() {
        let dir = tempdir().unwrap();
        fs::write(dir.path().join("a.py"), "").unwrap();
        fs::write(dir.path().join("b.py"), "").unwrap();
        fs::write(dir.path().join("main.py"), "").unwrap();
        assert_eq!(resolve_default_script(dir.path()), Ok("main.py".to_string()));
    }

    #[test]
    fn resolve_single_py_in_src() {
        let dir = tempdir().unwrap();
        fs::create_dir(dir.path().join("src")).unwrap();
        fs::write(dir.path().join("src/server.py"), "").unwrap();
        assert_eq!(resolve_default_script(dir.path()), Ok("src/server.py".to_string()));
    }

    #[test]
    fn resolve_src_main_py() {
        let dir = tempdir().unwrap();
        fs::create_dir(dir.path().join("src")).unwrap();
        fs::write(dir.path().join("src/a.py"), "").unwrap();
        fs::write(dir.path().join("src/main.py"), "").unwrap();
        assert_eq!(resolve_default_script(dir.path()), Ok("src/main.py".to_string()));
    }

    #[test]
    fn resolve_nothing_errors_with_four_attempts() {
        let dir = tempdir().unwrap();
        match resolve_default_script(dir.path()) {
            Err(attempts) => assert_eq!(attempts.len(), 4),
            Ok(p) => panic!("expected Err, got {}", p),
        }
    }

    #[test]
    fn run_plan_explicit_file() {
        let dir = tempdir().unwrap();
        assert_eq!(
            run_plan(dir.path(), Some("foo.py"), &[]),
            Plan::RunUv(vec![UvCmd::new(["run", "foo.py"])])
        );
    }

    #[test]
    fn run_plan_forwards_extra() {
        let dir = tempdir().unwrap();
        let extra = vec!["--port".to_string(), "8000".to_string()];
        assert_eq!(
            run_plan(dir.path(), Some("foo.py"), &extra),
            Plan::RunUv(vec![UvCmd::new(["run", "foo.py", "--port", "8000"])])
        );
    }
}
```

- [ ] **Step 2: Wire module + run tests** — temporarily add `mod plan; mod detect;` to `main.rs`, then:

Run: `cargo test detect`
Expected: all `detect` tests pass.

---

### Task 4: `cli.rs` — arg dispatch (TDD)

**Files:**
- Create: `src/cli.rs`

- [ ] **Step 1: Write `src/cli.rs`**

```rust
//! Map CLI args to a `Plan`. Pure: takes the args and the working dir.

use crate::detect;
use crate::plan::Plan;
use std::path::Path;

pub const UV_INSTALL_URL: &str = "https://docs.astral.sh/uv/getting-started/installation/";

pub const USAGE: &str = "\
uva — uv automations（让 Python 拥有 yarn 般的体验）

用法:
  uva                       安装依赖（等同于 uva install）
  uva install               安装当前项目的依赖
  uva run [文件] [参数...]    运行脚本（不传文件时自动寻找入口）
  uva start [文件] [参数...]  等同于 uva run
  uva <文件> [参数...]        若文件存在，等同于 uva run <文件>
  uva how-to-install-uv     输出 uv 的安装地址
  uva --help                显示本帮助
  uva --version             显示版本

提示: uva 始终使用 uv 当前激活的 Python 版本。要切换版本，请直接使用 uv。";

/// Map args (excluding program name) to a `Plan`, inspecting `dir`.
pub fn dispatch(args: &[String], dir: &Path) -> Plan {
    match args.split_first() {
        None => detect::install_plan(dir),
        Some((first, rest)) => match first.as_str() {
            "install" => detect::install_plan(dir),
            "run" | "start" => {
                let (filename, extra) = split_filename(rest);
                detect::run_plan(dir, filename, extra)
            }
            "how-to-install-uv" => Plan::PrintUrl,
            "--help" | "-h" => Plan::Help,
            "--version" | "-V" => Plan::Version,
            _ => {
                if dir.join(first).is_file() {
                    detect::run_plan(dir, Some(first), rest)
                } else {
                    Plan::Usage
                }
            }
        },
    }
}

fn split_filename(rest: &[String]) -> (Option<&str>, &[String]) {
    match rest.split_first() {
        Some((f, extra)) => (Some(f.as_str()), extra),
        None => (None, &[]),
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::plan::UvCmd;
    use std::fs;
    use tempfile::tempdir;

    fn s(v: &[&str]) -> Vec<String> {
        v.iter().map(|x| x.to_string()).collect()
    }

    #[test]
    fn no_args_is_install() {
        let dir = tempdir().unwrap();
        fs::write(dir.path().join("pyproject.toml"), "").unwrap();
        assert_eq!(dispatch(&[], dir.path()), Plan::RunUv(vec![UvCmd::new(["sync"])]));
    }

    #[test]
    fn install_keyword() {
        let dir = tempdir().unwrap();
        fs::write(dir.path().join("pyproject.toml"), "").unwrap();
        assert_eq!(dispatch(&s(&["install"]), dir.path()), Plan::RunUv(vec![UvCmd::new(["sync"])]));
    }

    #[test]
    fn how_to_install_uv() {
        let dir = tempdir().unwrap();
        assert_eq!(dispatch(&s(&["how-to-install-uv"]), dir.path()), Plan::PrintUrl);
    }

    #[test]
    fn run_explicit_file() {
        let dir = tempdir().unwrap();
        assert_eq!(
            dispatch(&s(&["run", "foo.py"]), dir.path()),
            Plan::RunUv(vec![UvCmd::new(["run", "foo.py"])])
        );
    }

    #[test]
    fn run_forwards_extra_args() {
        let dir = tempdir().unwrap();
        assert_eq!(
            dispatch(&s(&["run", "foo.py", "--port", "8000"]), dir.path()),
            Plan::RunUv(vec![UvCmd::new(["run", "foo.py", "--port", "8000"])])
        );
    }

    #[test]
    fn start_is_run() {
        let dir = tempdir().unwrap();
        assert_eq!(
            dispatch(&s(&["start", "foo.py"]), dir.path()),
            Plan::RunUv(vec![UvCmd::new(["run", "foo.py"])])
        );
    }

    #[test]
    fn bare_existing_filename_runs() {
        let dir = tempdir().unwrap();
        fs::write(dir.path().join("script.py"), "").unwrap();
        assert_eq!(
            dispatch(&s(&["script.py"]), dir.path()),
            Plan::RunUv(vec![UvCmd::new(["run", "script.py"])])
        );
    }

    #[test]
    fn bare_existing_filename_forwards_args() {
        let dir = tempdir().unwrap();
        fs::write(dir.path().join("script.py"), "").unwrap();
        assert_eq!(
            dispatch(&s(&["script.py", "arg1"]), dir.path()),
            Plan::RunUv(vec![UvCmd::new(["run", "script.py", "arg1"])])
        );
    }

    #[test]
    fn bare_missing_filename_is_usage() {
        let dir = tempdir().unwrap();
        assert_eq!(dispatch(&s(&["nope.py"]), dir.path()), Plan::Usage);
    }

    #[test]
    fn help_and_version_flags() {
        let dir = tempdir().unwrap();
        assert_eq!(dispatch(&s(&["--help"]), dir.path()), Plan::Help);
        assert_eq!(dispatch(&s(&["--version"]), dir.path()), Plan::Version);
    }
}
```

- [ ] **Step 2: Run tests**

Run: `cargo test cli`
Expected: all `cli` tests pass.

---

### Task 5: `runner.rs` — executor

**Files:**
- Create: `src/runner.rs`

- [ ] **Step 1: Write `src/runner.rs`**

```rust
//! Side-effecting executor: check `uv` presence, echo, spawn, propagate code.

use crate::cli::{UV_INSTALL_URL, USAGE};
use crate::plan::{Plan, UvCmd};
use std::path::Path;
use std::process::{Command, Stdio};

/// Execute a plan; return the process exit code uva should terminate with.
pub fn execute(plan: Plan) -> i32 {
    match plan {
        Plan::PrintUrl => {
            println!("{}", UV_INSTALL_URL);
            0
        }
        Plan::Help => {
            println!("{}", USAGE);
            0
        }
        Plan::Version => {
            println!("uva {}", env!("CARGO_PKG_VERSION"));
            0
        }
        Plan::Usage => {
            eprintln!("{}", USAGE);
            2
        }
        Plan::Fail(msg) => {
            eprintln!("uva: {}", msg);
            1
        }
        Plan::RunUv(cmds) => run_uv_sequence(cmds),
    }
}

fn run_uv_sequence(cmds: Vec<UvCmd>) -> i32 {
    if !uv_available() {
        eprintln!("uva: 未找到 uv。请先安装 uv：{}", UV_INSTALL_URL);
        return 1;
    }
    for cmd in cmds {
        if cmd.skip_if_venv_exists && Path::new(".venv").exists() {
            continue;
        }
        eprintln!("$ uv {}", cmd.args.join(" "));
        match Command::new("uv").args(&cmd.args).status() {
            Ok(status) => {
                let code = status.code().unwrap_or(1);
                if code != 0 {
                    return code;
                }
            }
            Err(e) => {
                eprintln!("uva: 无法执行 uv: {}", e);
                return 1;
            }
        }
    }
    0
}

/// Whether `uv` is resolvable on PATH.
fn uv_available() -> bool {
    Command::new("uv")
        .arg("--version")
        .stdout(Stdio::null())
        .stderr(Stdio::null())
        .status()
        .is_ok()
}
```

---

### Task 6: `main.rs` — wire it together

**Files:**
- Modify: `src/main.rs`

- [ ] **Step 1: Replace `src/main.rs`**

```rust
mod cli;
mod detect;
mod plan;
mod runner;

use std::path::PathBuf;
use std::process::exit;

fn main() {
    let args: Vec<String> = std::env::args().skip(1).collect();
    let cwd = std::env::current_dir().unwrap_or_else(|_| PathBuf::from("."));
    let plan = cli::dispatch(&args, &cwd);
    exit(runner::execute(plan));
}
```

- [ ] **Step 2: Full build + test**

Run: `cargo build && cargo test`
Expected: builds clean; all unit tests pass.

- [ ] **Step 3: Lint**

Run: `cargo clippy --all-targets -- -D warnings`
Expected: no warnings.

---

### Task 7: Integration smoke tests

**Files:**
- Create: `tests/smoke.rs`

- [ ] **Step 1: Write `tests/smoke.rs`**

```rust
use std::process::Command;

fn uva() -> Command {
    Command::new(env!("CARGO_BIN_EXE_uva"))
}

#[test]
fn how_to_install_uv_prints_url() {
    let out = uva().arg("how-to-install-uv").output().unwrap();
    assert!(out.status.success());
    assert_eq!(
        String::from_utf8_lossy(&out.stdout).trim(),
        "https://docs.astral.sh/uv/getting-started/installation/"
    );
}

#[test]
fn install_outside_python_project_fails() {
    let dir = tempfile::tempdir().unwrap();
    let out = uva().arg("install").current_dir(dir.path()).output().unwrap();
    assert!(!out.status.success());
    assert!(String::from_utf8_lossy(&out.stderr).contains("Python"));
}

#[test]
fn bare_unknown_arg_shows_usage_exit_2() {
    let dir = tempfile::tempdir().unwrap();
    let out = uva().arg("definitely-not-a-file").current_dir(dir.path()).output().unwrap();
    assert_eq!(out.status.code(), Some(2));
    assert!(String::from_utf8_lossy(&out.stderr).contains("用法"));
}

/// Real end-to-end run through uv. Ignored by default because it may make uv
/// download a managed Python the first time. Run with `cargo test -- --ignored`.
#[test]
#[ignore]
fn run_executes_script_via_uv() {
    let dir = tempfile::tempdir().unwrap();
    std::fs::write(dir.path().join("hello.py"), "print('hello from uva')").unwrap();
    let out = uva().args(["run", "hello.py"]).current_dir(dir.path()).output().unwrap();
    assert!(out.status.success());
    assert!(String::from_utf8_lossy(&out.stdout).contains("hello from uva"));
}
```

- [ ] **Step 2: Run smoke tests**

Run: `cargo test --test smoke`
Expected: the three non-ignored tests pass.

---

### Task 8: README + manual end-to-end verification + stage

**Files:**
- Create: `README.md`

- [ ] **Step 1: Write `README.md`** documenting install, the command table, the venv/Python philosophy.

- [ ] **Step 2: Manually verify the real uv path** (the part automated tests skip)

Build the release binary and, in a throwaway temp project, confirm:
- `uva run hello.py` actually runs the script through `uv run` and prints output.
- `uva install` against a minimal `pyproject.toml` runs `uv sync` and creates `.venv` without the user ever naming an env.

- [ ] **Step 3: Stage files (do NOT commit — user commits)**

Run: `git add -A`
Expected: files staged; no commit made.

---

## Self-Review

- **Spec coverage:** `install` priority (uv.lock/pyproject→sync, requirements→venv+pip, else fail) → Task 3. `run`/`start`/bare-file/default-resolution → Tasks 3-4. `how-to-install-uv` → Task 4. echo to stderr, uv pre-check, exit codes, venv abstraction → Task 5. Python-version note → USAGE text in Task 4. All covered.
- **Placeholder scan:** none — every code step is complete.
- **Type consistency:** `Plan` variants and `UvCmd::new` / `skip_if_venv_exists` are used identically across `detect`, `cli`, and `runner`. `dispatch(args, dir)` and `run_plan(dir, filename, extra)` signatures are consistent between definition and call sites.
